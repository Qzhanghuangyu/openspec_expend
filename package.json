{
  "name": "falla-openspec",
  "version": "0.3.0",
  "private": true,
  "description": "Falla workflow extensions for the official OpenSpec CLI",
  "type": "module",
  "bin": {
    "falla-openspec": "./bin/falla-openspec.js"
  },
  "files": [
    "bin",
    "src",
    "templates",
    "README.md"
  ],
  "scripts": {
    "check": "find bin src test -name '*.js' -type f -exec node --check '{}' ';'",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "yaml": "2.9.0"
  },
  "peerDependencies": {
    "@fission-ai/openspec": ">=1.12.0 <1.13.0"
  },
  "devDependencies": {
    "@fission-ai/openspec": "1.12.0"
  }
}
