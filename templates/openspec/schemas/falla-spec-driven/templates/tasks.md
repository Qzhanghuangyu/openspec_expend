# 任务

## 执行模式

- 模式：single
- 说明：默认由父 change 直接实施；只有用户明确要求并行分派时才改为 parallel 并创建子 change。

## 1. 契约与 ViewModel

- [ ] 1.1 定义可验证的数据与状态契约（依赖：无）

## 2. UI 模块控件

- [ ] 2.1 搭建独立控件框架（依赖：1.1）

## 3. 组装与验证

- [ ] 3.1 组装页面并执行功能验证（依赖：2.1）
- [ ] 3.2 覆盖空值、异常、弱网和重复操作（依赖：3.1）
- [ ] 3.3 验证协程、Flow、观察者、回调和监听器按生命周期取消或释放，且页面销毁后不再更新 UI（依赖：3.1）
- [ ] 3.4 检查日志、缓存、网络请求和产物不包含凭据或未脱敏的敏感信息（依赖：3.1）
- [ ] 3.5 记录验证命令、variant/设备、结果和未通过项；不适用项写明原因（依赖：3.2, 3.3, 3.4）
- [ ] 3.6 记录需要人工校准的视觉项（依赖：3.1）
