# 任务

> 执行模式与验证模式只记录在 `comate.md`。本文件只保存当前 change 的具体任务和 checkbox 进度。
> 删除不适用的占位项，并把每项任务改写为可独立定位、修改、验证和交接的实际工作。
> apply 一次只推进一个 ready task；完成并验证后立即勾选，未完成时不得提前或批量补勾。
> 新增或实质修改代码的任务须逐符号审计注释。
> 普通任务只做当前所需轻量检查，将待集成结果指向后续集成任务；交付可运行入口时最终构建应用 APK。
> Figma UI 复现保留关键节点文本核对与独立 `[人工]` 视觉验收；不以编译或层级检查代替视觉结论。

## 1. 结构与契约

- [ ] 1.1 <任务>（输入：；允许编辑：；完成条件：；依赖：无）

## 2. 实施

- [ ] 2.1 <任务>（输入：；允许编辑：；完成条件：；依赖：1.1）

## 3. 集成验证（有构建影响时保留独立任务；无构建影响的定向验证可写入实施任务完成条件）

- [ ] 3.1 <相关实施完成后的集成验证；可运行入口仅构建一次实际应用 APK，无应用时定向验证模块；
  UI 复现能运行时核对当前页按宽适配、实际内容高度/Insets 与 design 关键节点，并记录退出检查；
  无法运行时将必要核对移至 4.1，
  不绑在构建任务上，不预排全量矩阵>（依赖：2.1）

## 4. 人工验证

- [ ] 4.1 [人工] <Agent 无法执行但完成条件必需的节点文本核对、退出实测或联调；写明步骤、预期结果、
  设备配置和依赖>（依赖：2.1；保留 3.1 时改为 3.1）
- [ ] 4.2 [人工] <Figma UI 复现的视觉验收：指定代表性页面/状态、设备配置、步骤和预期结果；
  反馈只记录区域、预期/实际差异及通过/待改，不向模型传截图>（依赖：2.1；保留 3.1 时改为 3.1）
