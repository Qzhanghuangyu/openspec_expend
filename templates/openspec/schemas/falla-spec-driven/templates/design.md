# 设计

## 上下文

## 目标 / 非目标

## 页面实现结构基线

> 新建或重构页面时填写；非页面变更写明不适用。

- 同类页面、可复用组件与源码证据：
- 页面承载方式、入口及文件归属：
- 页面结构（根节点、滚动与状态容器；Compose 页面在此说明）：
- 导航、参数、资源、主题与 Insets：
- ViewModel 作用域及状态、事件、UI 绑定：
- 根页面/XML 修改责任与生命周期所有者：

### XML 布局结构（使用 XML 的页面）

> 按页面列出实际布局文件（已有或拟新增）和关键节点的父子关系，方便人工改方案。
> 填到根容器、主要内容、滚动和状态容器即可；重复项可合并，不写完整 XML 属性、样式或每个控件。
> 以下只是格式，生成时替换占位符；现有结构须核对源码，拟新增节点标明“拟新增”，未确定的留待确认，不猜类名或 id。
> 纯 Compose 或非页面变更写“不适用”。

```text
res/layout/<页面>.xml（已有 / 拟新增）
<根布局类型> [id: ...]
├─ <顶部区域类型> [id: ...]
├─ <滚动或内容容器类型> [id: ...]
│  └─ <主体容器类型> [id: ...]
└─ <状态容器类型> [id: ...]（如适用）
```

## 设计源证据（仅使用 Figma 时）

> 从 `preflight.md` 的“设计节点交接”接手已授权的节点；Propose 新收到的节点也在此记录。
> 不依赖旧对话；不保存原始链接、查询参数、凭据、临时资源 URL、截图或完整设计正文。
> 不使用 Figma 时写“不适用”。授权只限当前 change 和已记录节点，不自动扩展到整个设计文件。

- 授权：用户确认日期、当前 change 和节点范围；来源为 Preflight / Propose：
- 节点：逐个列出 file key、精确 node id、状态（未核对 / 已核对 / 失败）及最后核对日期：
- 实施所需事实：页面/组件结构、状态与交互等必要结论；无法确定的视觉细节留给人工校准：
- 资源：逐项列出项目内相对路径、下载状态及本地普通文件哈希；拒绝越界路径和符号链接，缺失或哈希不符时重新核对：
- 未决项、已知变化与重新核对条件：

## 项目规则审计

> 如果 `.falla/project-rules/` 存在，按文件名读取全部顶层普通 `.md`。每条 required 都必须列出，
> 不适用也要记录具体条件和证据；适用 required 必须转成 tasks。

| Rule ID | 级别 | 适用性 | 命中证据或不适用原因 | 落地方式 | 例外及批准依据 |
| --- | --- | --- | --- | --- | --- |
|  | required / preferred / reference-only | 适用 / 不适用 / 冲突 / 已批准例外 |  |  |  |

## 已确认实现基线

> 设计中引用的知识或源码证据必须落实到具体实现对象。`required` 必须遵守；`preferred` 偏离时说明；
> `reference-only` 只参考思路。引用知识条目本身不自动成为 required。

| 实现对象 | Knowledge / 源码证据 | 约束级别 | 选定类、基类或 API | 禁止替代 | 例外处理 |
| --- | --- | --- | --- | --- | --- |
|  |  | required / preferred / reference-only |  |  | 返回 propose / 记录偏差 |

## 注释与可维护性契约

> 只列当前 change 新增或实质修改的符号。短方法不能自动豁免；纯转发且无副作用、无状态与
> 生命周期语义的 override/getter/setter/委托才可注明具体豁免原因。

| 文件 / 符号 | 类型 | 必须说明的职责或语义 | 参数 / 生命周期要求 | 豁免原因 |
| --- | --- | --- | --- | --- |
|  | 类 / 方法 / 字段 / 常量 / Lambda |  |  |  |

## MVVM 与组件边界

## 关键决策与替代方案

## UI 框架与人工校准

- AI 框架范围：
- 人工校准范围：间距手感、字体渲染、动效、机型适配和设计隐性意图。

## 生命周期与状态恢复

- 状态、异步任务与订阅的所有者：
- 创建 / 启动时机：
- 取消 / 释放时机：
- 前后台切换与进程重建策略：
- 一次性事件的重复消费防护：
- 页面销毁后更新 UI：禁止；验证方式：
- 静态生命周期核对与必要的运行时退出检查：分别说明验证条件；无法实测时的 `[人工]` 项：

## 安全与敏感信息

- 敏感数据范围与最小化原则：
- 日志与上报的脱敏策略：
- 本地缓存与网络传输保护：
- 凭据、认证信息和临时资源的处理：

## 风险 / 权衡

## 迁移与回滚

## 开放问题
