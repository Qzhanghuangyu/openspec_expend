interface:
  display_name: "Falla Propose"
  short_description: "把已分析需求规划为单 change，按需启用并行子 change"
  default_prompt: "Use $falla-propose to plan this preflighted change in single mode by default; create child changes only when I explicitly request parallel execution."
